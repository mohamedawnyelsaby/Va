'use client';

// Temporarily disabled Pi Network integration
// TODO: Install correct Pi Network SDK when available

export function PiProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
