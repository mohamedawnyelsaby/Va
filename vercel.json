{
  "buildCommand": "npm install typescript @types/react @types/node --legacy-peer-deps && prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  },
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "1",
      "SKIP_PRISMA_MIGRATE": "1"
    }
  }
}
